name: Auto approve

on: pull_request_target

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: github.actor == 'github.actions[bot]'
    steps:
      - uses: kirankotari/auto-approve-action@v4
